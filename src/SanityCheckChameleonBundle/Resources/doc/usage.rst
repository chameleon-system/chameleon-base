Usage
=====

The bundle adds a backend module that is accessible via the new Chameleon backend main menu entry `SanityCheck`.
When clicking that link, a window opens which allows to execute arbitrary checks.

See the ChameleonSystemCoreBundle for examples on how to create checks.