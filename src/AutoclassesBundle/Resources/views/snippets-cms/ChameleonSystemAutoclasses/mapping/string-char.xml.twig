{# @var definition array #}
<field name="{{ fieldName }}" type="{{ type }}" column="{{ column }}" length="{{ length }}" nullable="false" >
    <options>
        <option name="fixed">true</option>
        <option name="comment">{{ comment }}</option>
        <option name="default">{{ default }}</option>
    </options>
</field>