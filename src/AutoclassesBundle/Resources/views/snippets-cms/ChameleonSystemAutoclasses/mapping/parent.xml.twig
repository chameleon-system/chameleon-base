<many-to-one field="{{ fieldName }}" target-entity="{{ targetClass }}" {% if owningPropertyName %}inversed-by="{{ owningPropertyName }}"{% endif %}>
    <join-column name="{{ definition.name }}" referenced-column-name="id" />
</many-to-one>